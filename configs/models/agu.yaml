agu:
  name: "AdaptiveGatingUnit"
  hidden_dim: 128
  num_adapters: 4
  conditioning:
    - "timestep"
    - "text_embedding"
  
  gates:
    activation: "sigmoid"
    bias: 0.5 # Initialize slightly open
  
  regularization:
    sparsity_weight: 0.01
